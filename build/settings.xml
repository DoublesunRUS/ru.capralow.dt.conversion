<settings>
	<servers>
		<server>
			<id>dt_repository</id>
			<username>${env.DT_REPO_USERNAME}</username>
			<password>${env.DT_REPO_PASSWORD}</password>
		</server>
	</servers>
	
	<mirrors>
	</mirrors>

	<proxies>
	</proxies>
</settings>
